# Web Package Note

The current MVP web app lives at repository root under `src/` and uses the root `package.json` / `package-lock.json`. This folder keeps a copied web lockfile for AI4I evidence and future monorepo migration.
